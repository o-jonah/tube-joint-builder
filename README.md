# Tube Joint Builder

Minimal Vite + React + TypeScript scaffold.

Getting started

Install dependencies:

```powershell
npm install
```

Run dev server:

```powershell
npm run dev
```

Build for production:

```powershell
npm run build
npm run preview
```
